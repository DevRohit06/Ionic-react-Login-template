import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.classes.app',
  appName: 'class',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
